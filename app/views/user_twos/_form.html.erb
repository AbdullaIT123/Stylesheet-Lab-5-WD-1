<%= form_with(model: user_two, local: true) do |form| %>
  <% if user_two.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_two.errors.count, "error") %> prohibited this user_two from being saved:</h2>

      <ul>
      <% user_two.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :user_two_name %>
  </div>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email, id: :user_two_email %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
